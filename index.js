const create = require("./src/create");
const store = require("./src/store");
const init = require("./src/init");

const lib = {
    create: create,
    store: store,
    init: init
};

module.exports = lib;